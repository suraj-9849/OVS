<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login-Online Voting System</title>
    <link rel="stylesheet" href="../stylesheets/login.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/locomotive-scroll@3.5.4/dist/locomotive-scroll.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100vh;
            overflow: hidden;
        }
        .flex-container {
            height: 100%;
            overflow: auto;
            scrollbar-width: none; /* For Firefox */
            -ms-overflow-style: none; /* For Internet Explorer and Edge */
        }
        .flex-container::-webkit-scrollbar {
            display: none; /* For Chrome, Safari, and Opera */
        }
        .form-label {
            font-size: 0.9rem;
            margin-bottom: 0.25rem;
        }
        .form-input {
            padding: 0.5rem;
            font-size: 0.9rem;
            border-radius: 0.25rem;
        }
        .form-button {
            padding: 0.5rem;
            font-size: 0.9rem;
        }
    </style>
</head>

<body class="flex items-center justify-center ">
    <div class="flex-container bg-zinc-100 flex items-center justify-center shadow-lg ">
        <div class="bg-white p-6 rounded-lg shadow-md w-full max-w-md">
            <h1 class="text-3xl font-extrabold text-center mb-4">baburao</h1>
            <h2 class="text-2xl font-extrabold text-center mb-4">Register</h2>
            <p class="text-center text-gray-600 mb-4">Signup now and get access to vote.</p>

            <form id="registrationForm">
                <div class="grid grid-cols-2 gap-2 mb-2">
                    <div>
                        <label class="form-label text-sm text-gray-600">Firstname</label>
                        <input class="form-input w-full border" type="text" required>
                    </div>
                    <div>
                        <label class="form-label text-sm text-gray-600">Lastname</label>
                        <input class="form-input w-full border" type="text" required>
                    </div>
                </div>

                <div class="mb-2">
                    <label class="form-label text-sm text-gray-600">Email</label>
                    <input class="form-input w-full border" type="email" required>
                </div>

                <div class="mb-2">
                    <label class="form-label text-sm text-gray-600">Adharcard Number</label>
                    <input class="form-input w-full border" type="text" required>
                </div>

                <div class="mb-2">
                    <label class="form-label text-sm text-gray-600">Pan-card Number</label>
                    <input class="form-input w-full border" type="text" required>
                </div>

                <div class="mb-2">
                    <label class="form-label text-sm text-gray-600">Password</label>
                    <input class="form-input w-full border" type="password" required>
                </div>

                <div class="mb-4">
                    <label class="form-label text-sm text-gray-600">Confirm password</label>
                    <input class="form-input w-full border" type="password" required>
                </div>

                <button class="form-button w-full bg-blue-500 text-white rounded-md hover:bg-blue-600 focus:outline-none focus:border-blue-700" type="submit">Submit</button>
            </form>

            <p class="text-center text-gray-600 mt-2">Already have an account? <a href="#" class="text-blue-500">Sign in</a></p>
        </div>
    </div>

    <script>
        document.getElementById('registrationForm').addEventListener('submit', function(event) {
            event.preventDefault(); 
            window.location.href = '/home';
        });
    </script>
</body>

</html>
